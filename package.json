{
  "name": "culvert",
  "version": "1.0.0",
  "private": true,
  "description": "",
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "build:all": "npm run build -ws"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "vitest": "^2.0.0",
    "@types/node": "^25.5.2"
  }
}
