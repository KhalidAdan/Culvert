{
  "name": "culvert",
  "version": "1.0.1",
  "private": true,
  "description": "",
  "type": "module",
  "workspaces": [
    "packages/*",
    "packages/internal/*"
  ],
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "build:all": "npm run build -ws",
    "ex:tar-peek-dev": "cd apps/examples/tar-peek/ && npm run dev"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "vitest": "^2.0.0",
    "@types/node": "^25.5.2"
  }
}
